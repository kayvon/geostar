{
  "name": "geostar-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create geostar",
    "db:migrate": "wrangler d1 execute geostar --file=schema.sql",
    "db:migrate:local": "wrangler d1 execute geostar --local --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.99.0"
  }
}
